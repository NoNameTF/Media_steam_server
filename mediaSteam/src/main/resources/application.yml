server:
  port: 8080

logging:
  level:
    root: error
    com:
      noname:
        mediasteam: debug
    org:
      hibernate:
        SQL: trace

spring:
  profiles:
    group:
      local: localdb, oauth